!!!
%html
  %head
    %title Tic Tac Toe
    %link{:href => url("/styles.css"), :rel => "stylesheet", :type => "text/css"}

  %body
  %h1 Tic Tac Toe
  .board
    - @board.rows.each do |row|
      .row
        .wrapper
          - if @board.available_positions.include? row[0]
            %a.cell{:href => url("/move?move=#{row[0]}")}
              .empty
                &nbsp;
          - else
            .cell.full
              = row[0]
        .wrapper
          - if @board.available_positions.include? row[1]
            %a.cell{:href => url("/move?move=#{row[1]}")}
              .empty
                &nbsp;
          - else
            .cell.full
              = row[1]
        .wrapper
          - if @board.available_positions.include? row[2]
            %a.cell{:href => url("/move?move=#{row[2]}")}
              .empty
                &nbsp;
          - else
            .cell.full
              = row[2]

  .game-end-message
    - if @draw == true
      %p Game over! It's a draw.
    - elsif @winner_mark != nil
      %p Game over! Winner is #{@winner_mark}.
